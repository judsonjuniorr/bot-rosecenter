// class AllowedChannel {
//   public allowed(channel): boolean {
//     const { name, guild } = channel as TextChannel;
//     const allowedChannels = ['rosecenter', 'rc-staff'];
//     return !(
//       !allowedChannels.includes(name) && guild.id !== '699698186734010468'
//     );
//   }
// }

// export default new AllowedChannel();
